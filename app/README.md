# Application package

Phase 0 will add the agent runtime abstractions here.

Planned boundaries:

- `agents/` — agent implementations
- `orchestrator/` — workflow coordination
- `tools/` — bounded capabilities
- `policies/` — permissions and execution rules
- `schemas/` — typed task/state/result models
